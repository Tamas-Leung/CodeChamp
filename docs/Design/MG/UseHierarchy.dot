digraph {
    node [shape = rectangle;];
    ClientT -> LobbyService;
    ClientT -> Router;
    GameT -> ClientT;
    MatchT;
    UserT;
    UserStatsT;
    ProblemT -> Difficulty;
    ProblemT -> TestCaseT;
    Difficulty;
    TestCaseT;
    SubmissionT -> Language;
    JudgeResultT -> JudgeVerdict;
    JudgeResultT -> TestCaseVerdictT;
    JudgeVerdict;
    TestCaseVerdictT -> TestCaseT;
    HomePage -> WebSocketService;
    ProfilePage -> Router;
    ProfilePage -> User;
    LeaderboardPage -> Router;
    LeaderboardPage -> User;
    LobbyPage -> WebSocketService;
    GamePage -> ProblemService;
    GamePage -> SubmissionService;
    LoginPage -> AuthService;
    SubmissionService -> Judge;
    ProblemService -> Problems;
    UserService -> User;
    AuthService -> Auth;
    AuthService -> User;
    AuthService -> Router;
    LobbyService;
    WebSocketService -> GameHandler;
    WebSocketService -> User;
    GameHandler -> GameT;
    GameHandler -> Router;
    GameHandler -> Problems;
    Judge -> CodeRunner;
    Judge -> GameHandler;
    Judge -> JudgeResultT;
    CodeRunner -> SubmissionT;
    CodeRunner -> TestCaseVerdictT;
    Problems -> Database;
    User -> Database;
    Database -> ProblemT;
    Database -> UserStatsT;
    Database -> UserT;
    Database -> MatchT;
    Router;
}
